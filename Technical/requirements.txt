# Stock Technical Analysis Tool - Python Dependencies
# ====================================================
# Install all dependencies with: pip install -r requirements.txt

# Core data processing
pandas>=2.0.0
numpy>=1.24.0

# Optional: Technical Analysis library (alternative to manual calculation)
# ta>=0.11.0

# Console table formatting
tabulate>=0.9.0

# Database (usually included with Python)
# sqlite3 is part of Python standard library

# Note: If you prefer using a technical analysis library instead of manual
# calculations, uncomment the 'ta' line above and modify the script accordingly.
